name: water_jug_challenge
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  get: ^4.6.5
  responsive_framework: ^0.2.0
  google_fonts: ^6.1.0
  smooth_page_indicator: ^1.1.0
  simple_animations: ^4.0.2
  
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
